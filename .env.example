# Get a token from @BotFather in Telegram, then copy this file to `.env`
# and paste the token below.
BOT_TOKEN=123456789:AAabCdeFGhiJKLmnoPQRstuVWXyz1234567

# Free API key for personalized AI forecasts — get one at
# https://aistudio.google.com/apikey (no credit card required).
# Optional: without it, the bot still runs and uses a static fallback text
# instead of calling Gemini.
GEMINI_API_KEY=
